<?php

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateContractScheduleEditsTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('41_contract_schedule_edits', function (Blueprint $table) {
            // $table->increments('id');
            // $table->integer('refund_id')->unsigned()->index();
            // $table->foreign('refund_id')->references('id')->on('2_refunds')->onDelete('cascade');
            // //$table->integer('order');           
            // $table->date('contract_edit_date');
            // $table->date('contract_new_start_date')->nullable();;
            // $table->date('contract_new_end_date')->nullable();;
            // $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        // Schema::dropIfExists('contract_schedule_edits');
    }
}
